{
  "name": "node-aula",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node index",
    "test": "jest --watchAll",
    "test:ci": "jest --ci --coverage --coverageReporters=html --coverageReporters=lcov"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.0.1",
    "supertest": "^4.0.2"
  }
}
